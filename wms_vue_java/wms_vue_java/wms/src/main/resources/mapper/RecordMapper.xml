<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ren.mapper.RecordMapper">
    <select id="pageC" resultType="com.ren.entity.RecordRes">
        select a.*,b.name goodslistname,c.name storagename,d.name goodsname,
               (select u.name from user u where u.id=a.userId)username,
               (select u.name from user u where u.id=a.adminId)adminname
        from record a,goodslist b ,storage c ,goods d
        ${ew.customSqlSegment}
    </select>
</mapper>
